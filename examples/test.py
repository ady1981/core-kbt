import json

from common import vfilter, remove_markdown_code, list_files, calc_standard_document_paths2

# adict = {'a': 1, 'b': 2}
# print(json.dumps(vfilter(lambda k, v: v > 0, adict), indent=2))
# print(list_files('../../documentation/docs/Дизайн', 'Стандарт -*'))
print({'a': 'b'} | {})